# Environment
.env
.venv/
venv/

# Local-only artifacts (large or transient)
data/price_cache.parquet
data/price_cache.pkl
data/cache/
data/failed_analysis_*.txt
data/last_run.log
# universe.csv is regenerated by seed_universe.py, no need to track
config/universe.csv

# NOTE: the following ARE tracked in git so the system is portable:
#   data/pipeline.db     — watchlist + history
#   data/reports/        — daily markdown reports
#   data/pending/        — dossiers waiting on analysis
#   data/analyzed/       — completed analyses

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
.DS_Store
